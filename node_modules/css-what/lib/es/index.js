import * as types from "./types";
import { isTraversal, parse } from "./parse";
import { stringify } from "./stringify";

// Re-export types
export * from "./types";

// Re-export parse functions
export { isTraversal, parse };

// Export stringify function
export { stringify };

// Add type annotations for better type checking
export type {
  Node,
  Attribute,
  Text,
  Comment,
  Document,
  Element,
  Root,
  Children,
  EmptyChildren,
  Traversal,
} from "./types";
