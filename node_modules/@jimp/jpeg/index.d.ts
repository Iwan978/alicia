import { DecoderFn, EncoderFn } from "@jimp/core";

type MimeType = "image/jpeg";

interface JpegClass {
  _quality: number;
  quality: (n: number, cb?: (err: Error | null, image: this) => void) => this;
}

interface Jpeg {
  mime: {
    "image/jpeg": MimeType[];
  };

  constants: {
    MIME_JPEG: MimeType;
  };

  encoders: {
    "image/jpeg": EncoderFn<Buffer, Jpeg>;
  };

  decoders: {
    "image/jpeg": DecoderFn<Buffer, Jpeg>;
  };

  class: JpegClass;
}

export default function(): Jpeg;
