import { Jimp, ImageCallback } from "@jimp/core";

interface CropOptions {
  tolerance?: number;
  cropOnlyFrames?: boolean;
  cropSymmetric?: boolean;
  leaveBorder?: number;
  ignoreSides?: {
    north: boolean;
    south: boolean;
    east: boolean;
    west: boolean;
  };
}

interface CropClass {
  crop(
    x: number,
    y: number,
    width: number,
    height: number,
    cb?: ImageCallback<this>
  ): this;
  cropQuiet(
    x: number,
    y: number,
    width: number,
    height: number,
    cb?: ImageCallback<this>
  ): this;
  autocrop(options?: CropOptions | number, cb?: ImageCallback<this>): this;
  autocrop(
    cropOnlyFrames?: boolean,
    cb?: ImageCallback<this>
  ): this;
}

interface Crop extends CropClass {
  class: CropClass;
}

export default function (): Crop;
