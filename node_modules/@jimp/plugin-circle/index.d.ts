import { ImageCallback } from "@jimp/core";

interface CircleOptions {
  radius: number;
  x: number;
  y: number;
}

interface Circle {
  circle(options?: CircleOptions | ImageCallback<this>): this;
  circle(cb: ImageCallback<this>): this;
}

export default function (): Circle {
  // implementation here
}
