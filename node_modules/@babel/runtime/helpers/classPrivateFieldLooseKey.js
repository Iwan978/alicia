const id = new WeakSet();

function _classPrivateFieldKey(name) {
  const key = `__private_${[...id][id.add(this)]}_${name}`;
  Object.defineProperty(this, key, { configurable: true, writable: true });
  return key;
}

module.exports = {
  _classPrivateFieldKey,
  default: _classPrivateFieldKey,
};


const { _classPrivateFieldKey } = require('./my-module');
// or
const myModule = require('./my-module');
const { _classPrivateFieldKey } = myModule;
