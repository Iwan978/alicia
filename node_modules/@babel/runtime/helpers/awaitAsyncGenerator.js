class OverloadYield {
  constructor(generator, index) {
    this.generator = generator;
    this.index = index;
  }

  async next(value) {
    this.index++;
    return { value, done: this.generator.next(value).done };
  }

  async return(value) {
    return { value: this.generator.return(value).value, done: true };
  }

  async throw(error) {
    return { value: this.generator.throw(error).value, done: true };
  }
}

module.exports = OverloadYield;
module.exports.__esModule = true;


const OverloadYield = require('./OverloadYield.js');

async function _awaitAsyncGenerator(generator) {
  return new OverloadYield(generator, 0);
}

module.exports = _awaitAsyncGenerator;
module.exports.__esModule = true;
