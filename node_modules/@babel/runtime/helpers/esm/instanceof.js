export default function isInstanceOf(left, right) {
  // Check if right is a constructor with a Symbol.hasInstance method
  if (right != null && typeof right[Symbol.hasInstance] === 'function') {
    return right[Symbol.hasInstance](left);
  }

  // Fallback to using the instanceof operator
  return left instanceof right;
}
